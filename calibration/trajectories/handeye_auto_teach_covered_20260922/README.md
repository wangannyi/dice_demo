# 自动手眼标定示教轨迹

这组轨迹来自 2026-09-22 的人工手眼标定，共包含 20 个停稳采样姿态。按 10° 最大关节步长生成计划后包含 181 个路径点。仓库保存关节反馈、示教路径、可见范围和当时采用的参考标定结果，不保存采样图片。

## 内容

| 文件 | 用途 |
| --- | --- |
| `manifest.json` | 相机、裁剪、标定板和 TCP 信息 |
| `board_window.json` | 手背标定板允许出现的图像范围 |
| `teaching_frames.jsonl` | 人工移动期间记录的逐帧关节反馈 |
| `teaching_poses.json` | 20 个停稳采样姿态 |
| `sample_*.json` | 每个采样姿态的关节和板位姿记录 |
| `calibration_reference.json` | 生成自动轨迹时使用的参考手眼结果 |

参考结果的质量标志为 `false`，是现场明确接受误差后保留的临时结果，因此生成计划时必须显式传入 `--allow-provisional`。计划文件包含机器绝对路径和源文件哈希，不能跨目录复制；应按[标定指南](../../../docs/CALIBRATION.md#42-使用仓库内置轨迹)在目标机器上重新生成。

## 使用条件

仅在以下条件均保持一致时复用：

- NERO 基座、D435i 和手背标定板的安装关系未改变。
- 相机序列号为 `346222071954`。
- 彩色源图为 1280×720，裁剪为 `[220, 0, 960, 720]`。
- 使用 4×5 手背板，实测四格总宽 86.5 mm、五格总高 108 mm。

任一安装关系、相机分辨率或裁剪改变后，应重新人工示教并生成新轨迹。
